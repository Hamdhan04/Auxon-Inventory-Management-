[project]
name = "auxon-inventory-management"
version = "0.1.0"
description = "RL-based Inventory Optimization using OpenEnv"
authors = [
    { name="Mohamed Hamdhan" }
]
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "numpy"
]

[tool.setuptools]
packages = ["."]